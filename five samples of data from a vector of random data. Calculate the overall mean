x=randi(10,1,10);
c=mean (x);
for i=0:1:5
  w=randi(10,1,5);
if (y >=c)
  disp('greater than the overall mean ')
elseif (y=c)
disp('mean of the samples is equal to overall mean.')
else
  disp('mean of the samples is lessthan the overall mean..')
end
end
