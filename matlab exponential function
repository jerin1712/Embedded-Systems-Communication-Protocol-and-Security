Q2. Create a 3D plot of a surface by creating a grid along the X and Y axes and plotting the Z-coordinate according to the exponential function.
[x,y]=meshgrid(5:.2:10);
z=exp(x.^5 - y.^10);
surf(x,y,z);
